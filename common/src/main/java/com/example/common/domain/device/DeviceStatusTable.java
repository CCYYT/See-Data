package com.example.common.domain.device;

/**
 * 设备状态 数值映射表
* */
public class DeviceStatusTable {
    public static Integer OFF_LINE = 0; // 离线
    public static Integer ON_LINE = 1;  // 在线
    public static Integer DISABLED = 2;  // 未启用
}
